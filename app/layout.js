export const metadata = {
  title: "Construtor de Prompts IA",
  description: "Assistente interativo para construir prompts complexos e robustos",
};

import "./globals.css";

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>
        {children}
      </body>
    </html>
  );
}
